# Copy this file to `.env` locally and fill in the keys. DO NOT commit your real keys.
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=
GROQ_API_KEY=
OPENAI_API_KEY=
TAVILY_API_KEY=

# Example: set via your host's secrets / env var configuration (Render, Cloud Run, GitHub Secrets, etc.)
